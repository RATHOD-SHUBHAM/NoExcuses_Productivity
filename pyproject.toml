[project]
name = "noexcuses-productivity"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "nodejs-bin>=18.4.0a4",
]

[tool.uv.workspace]
members = ["backend"]
