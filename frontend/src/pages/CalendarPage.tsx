import { CalendarView } from "../components/calendar/CalendarView";

/** Standalone full-page calendar (same UI as the expandable block on Home). */
export function CalendarPage() {
  return <CalendarView showHero />;
}
