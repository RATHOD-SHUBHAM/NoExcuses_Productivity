# Copy to `frontend/.env` or set in Vercel → Settings → Environment Variables.
# Rebuild after changing (Vite bakes these at build time).

# Render (or other) backend — no trailing slash. Required in production.
# Example: https://noexcuses-api.onrender.com
VITE_API_BASE_URL=http://localhost:8000
